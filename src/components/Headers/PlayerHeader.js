const PlayerHeader = () => {
    return (
        <header className="player-header">
            <h1>Football fever</h1>
            <p>Select how many players, names, skill-level, and team-selection mode</p>
        </header>
    );
};

export default PlayerHeader;